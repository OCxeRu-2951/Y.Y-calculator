services:
  - type: web
    name: yy-calculator
    env: python
    region: Oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn --host 0.0.0.0 --port $PORT main:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.9
